#!/bin/sh
# §10: the golden tests run in under 2s and are wired to a pre-push hook.
# "When someone edits a constant at hour 19, this is what saves the demo."
set -e
echo "pre-push: running golden tests…"
cd "$(git rev-parse --show-toplevel)/api"
python -m pytest tests/golden -q
